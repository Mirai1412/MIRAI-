매계변수로 추가
android:orientation="vertical"
Toast 문구가 잠깐나오게해줌
ex)
    public void test(View view){
        Toast.makeText(getApplicationContext(), "확인"
                ,Toast.LENGTH_SHORT).show();
    }
.show() ->  화면에 나타나게 하라
경로 R.id.btn1
res밑에 아이디로 검색
Intent 페이지 전환시 필요한 정보를 Intent에 
담아서 전송
values 유지보수를위해 글과 색깔이 모여짐
drawable 이미지넣는다
manifests 앱의 기본설정들
자바코드에서 변수선언후 사용

ctral + o = override 해줌

android:layout_weight="1" 비율 설정

tv.setText(et.getText().toString()); 문자 전송

.showPrevious 이전화면
.showNext 다음화면

.setFlipInterval(1000); 1초마다 보여준다
.startFlipping 시작
.stopFlipping 끝

Fragment 상속 extends

ctral+O 상속받을 수 있는 메소드가나온다 
onCreate | onCreateView

return inflater.inflate(R.layout.fragment1,container, false);

android:singleLine="true"한줄만표시해달라//2줄x

android:layout_weight="1" //남아있는 공간을 자신으로 채운다

manifests 파일 환경설정
icon 로고
label 어플이름
android:theme="@style/Theme.AppCompat.Light.DarkActionBar"> 테마변경 
activity안에 라벨은 앱실행될시나오는 라벨

getSupportActionBar().setDisplayShowHomeEnabled(true);
getSupportActionBar().setIcon(R.drawable.yju_logo);
자바코드에 넣어서 위에 이미지가 뜨도록한다